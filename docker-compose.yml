services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pm-mvp-app
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - pm_data:/app/data

volumes:
  pm_data:
